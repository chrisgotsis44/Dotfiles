hl.config({
    general = {
        -- Decoration stuff
        gaps_in = 9,
        gaps_out = 18,

        -- Border
        border_size = 0,

        -- Miscellaneous
        resize_on_border = true,
        allow_tearing = false,
    },

    decoration = {
        -- Corner Rounding
        rounding = 12,
        rounding_power = 2,

        -- Change transparency of focused and unfocused windows
        active_opacity = 1.0,
        inactive_opacity = 0.85,
    },
})
